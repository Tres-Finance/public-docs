# API example:   init  

Runnable example script `core/__init__.py` from the public-docs repo (api-examples/).

```python
import logging
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(message)s'
)

```
